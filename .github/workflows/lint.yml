name: Java Code Linting
on:
  push:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Set up Java
        uses: actions/setup-java@v2
        with:
          java-version: '21' 
          distribution: 'adopt' 

      - name: Install Maven
        run: sudo apt-get install maven -y

      - name: Run Checkstyle Linting
        run: mvn checkstyle:check --file my-task-manager-app/pom.xml
